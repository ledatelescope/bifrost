---
name: "Nix Build"
"on": [push]
jobs:
  build:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2.4.0
      - name: Say hello
        run: |
          echo "Hello, world!"
          nvidia-smi
          nix run nixpkgs#neofetch
